import React, {Component} from 'react';
import {
    SafeAreaView,
    FlatList,
    Dimensions,
    Button,
    ScrollView,
    TextInput,
    View
} from 'react-native';

export default class Test extends Component{
    render(){
        return(
            <View>
                <TextInput/>
            </View>
        )
    }
}
